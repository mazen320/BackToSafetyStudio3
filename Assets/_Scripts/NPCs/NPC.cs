using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class NPC : Dialogue
{
    
    // Start is called before the first frame update
    void Start()
    {
        lines = new string[] { "Hello I am the level 1 npc", "I will show you the ropes of the game" };
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
